import styled from 'styled-components';

/* The Close Button */
const Close = styled.div`
	color: #aaa;
	float: right;
	font-size: 28px;
	font-weight: bold;

	&:hover{
	color: black;
	text-decoration: none;
	cursor: pointer;
	}
`;

export default Close;